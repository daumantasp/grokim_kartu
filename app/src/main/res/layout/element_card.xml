<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/root_frame_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <androidx.cardview.widget.CardView
        android:id="@+id/card_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_margin="10dp"
        app:cardCornerRadius="10dp">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:id="@+id/content_constraint_layout"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:foreground="?android:attr/selectableItemBackground"
            android:padding="10dp">

            <TextView
                android:id="@+id/title_text_view"
                style="@style/card_title"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                app:layout_constraintStart_toStartOf="@id/content_constraint_layout"
                app:layout_constraintTop_toTopOf="@id/content_constraint_layout"
                app:layout_constraintEnd_toEndOf="@id/content_constraint_layout"
                app:layout_constraintBottom_toTopOf="@id/description_text_view"/>

            <TextView
                android:id="@+id/description_text_view"
                style="@style/card_text"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                app:layout_constraintStart_toStartOf="@id/content_constraint_layout"
                app:layout_constraintTop_toBottomOf="@id/title_text_view"
                app:layout_constraintEnd_toEndOf="@id/content_constraint_layout"
                app:layout_constraintBottom_toBottomOf="@id/content_constraint_layout"/>

        </androidx.constraintlayout.widget.ConstraintLayout>
    </androidx.cardview.widget.CardView>
</FrameLayout>