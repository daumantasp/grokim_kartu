<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable
            name="model"
            type="com.dauma.grokimkartu.viewmodels.registrationConfirm.RegistrationConfirmViewModelImpl" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        tools:context=".ui.registrationConfirm.RegistrationConfirmFragment">

        <TextView
            android:id="@+id/registrationSuccessfulTextView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/registrationConfirm_registrationSuccessful" />

        <Button
            android:id="@+id/okButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:onClick="@{() -> model.next()}"
            android:text="@string/registrationConfirm_okButton" />
    </LinearLayout>

</layout>

